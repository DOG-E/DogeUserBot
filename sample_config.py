# PLEASE STOP!
# DO NOT EDIT THIS FILE OR DELETE THIS FILE!
# Create a new config.py file in same directory and import, then extend this class.

from os import environ as ose
from typing import Set

from telethon.tl.types import ChatBannedRights


class Config(object):
    LOGGER = True

    # MUST NEEDED VARS
    # Set this value with your name
    ALIVE_NAME = ose.get("ALIVE_NAME", None)
    # Get the values for following 2 from https://my.telegram.org/apps
    APP_ID = int(ose.get("APP_ID", 6))
    API_HASH = ose.get("API_HASH") or None
    # Database url Heroku sets it automatically else get this from ElephantSQL
    DB_URI = ose.get("DATABASE_URL", None)
    # Get this value by running https://repl.it/@DogeUserBot/DogeString or # TODO
    STRING_SESSION = ose.get("STRING_SESSION", None)
    # Telegram Bot Token and Bot Username from @BotFather ( https://t.me/BotFather )
    BOT_TOKEN = ose.get("BOT_TOKEN", None)
    BOT_USERNAME = None
    # Get this value from http://www.timezoneconverter.com/cgi-bin/findzone.tzc
    TZ = ose.get("TZ", "Europe/Istanbul")
    # Set this with required Doge repository link
    UPSTREAM_REPO = ose.get("UPSTREAM_REPO", "https://github.com/DOG-E/DogeUserBot.git")

    # BASIC & MAIN CONFIG VARS
    # For profile default name
    AUTONAME = ose.get("AUTONAME", None)
    # Set this value with group ID of private group(can be found this value by .id)
    PRIVATE_GROUP_BOT_API_ID = int(ose.get("PRIVATE_GROUP_BOT_API_ID") or 0)
    # Set this value same as PRIVATE_GROUP_BOT_API_ID if you need PMGUARD
    PRIVATE_GROUP_ID = int(ose.get("PRIVATE_GROUP_ID") or 0)
    # Set this value for working of fban/unfban/superfban/superunfban cmd
    FBAN_GROUP_ID = int(ose.get("FBAN_GROUP_ID") or 0)
    # Set this value with channel ID of private channel use full for .frwd cmd
    PRIVATE_CHANNEL_BOT_API_ID = int(ose.get("PRIVATE_CHANNEL_BOT_API_ID") or 0)
    # For Heroku plugin you can get this value from https://dashboard.heroku.com/account
    HEROKU_API_KEY = ose.get("HEROKU_API_KEY", None)
    # Set this with same app name you given for Heroku
    HEROKU_APP_NAME = ose.get("HEROKU_APP_NAME", None)
    # Owner ID to show profile link of given ID as owner
    OWNER_ID = int(ose.get("OWNER_ID") or 0)
    # Set this with group ID so it keeps notifying about your tagged messages or PMs
    PM_LOGGER_GROUP_ID = int(
        ose.get("PM_LOGGER_GROUP_ID") or ose.get("PM_LOGGR_BOT_API_ID") or 0
    )

    # CUSTOM VARS
    # Set this will channel ID of your custom plugins
    PLUGIN_CHANNEL = int(ose.get("PLUGIN_CHANNEL") or 0)
    DOGEPLUGIN = ose.get("DOGEPLUGIN", False)
    DOGEHUB = ose.get("DOGEHUB", False)
    # Set this value with your required name for Telegraph plugin
    TELEGRAPH_SHORT_NAME = ose.get("TELEGRAPH_SHORT_NAME", "@DogeUserBot")
    # For custom thumb image set this with your required thumb Telegraph link
    THUMB_IMAGE = ose.get(
        "THUMB_IMAGE", "https://telegra.ph/file/6086da8c041f5de3227ed.jpg"
    )
    # Specify NO_LOAD with plugin names for not loading in userbot
    NO_LOAD = [x for x in ose.get("NO_LOAD", "").split()]
    # For custom pic for .digitalpfp
    DIGITAL_PIC = ose.get("DIGITAL_PIC", None)
    # Your default pic Telegraph link
    DEFAULT_PIC = ose.get("DEFAULT_PIC", None)
    # Set this with your default bio
    DEFAULT_BIO = ose.get("DEFAULT_BIO", None)
    # Set this with your default name
    DEFAULT_NAME = ose.get("DEFAULT_NAME", None)
    # Specify command handler that should be used for the plugins
    # This should be a valid "regex" pattern
    CMDSET = ose.get("CMDSET", r".")
    SUDO_CMDSET = ose.get("SUDO_CMDSET", r".")
    # Set this with required folder path to act as download folder
    TMP_DOWNLOAD_DIRECTORY = ose.get("TMP_DOWNLOAD_DIRECTORY", "downloads")
    # Set this with required folder path to act as temparary folder
    TEMP_DIR = ose.get("TEMP_DIR", "./temp/")
    # Time to update autoprofile cmds
    CHANGE_TIME = int(ose.get("CHANGE_TIME", 60))
    # SpamWatch, CAS, SpamProtection ban needed or not
    ANTISPAMBOT_BAN = ose.get("ANTISPAMBOT_BAN", False)
    # Is dual logging needed? Write; True or False
    DUAL_LOG = ose.get("DUAL_LOG", False)
    # Progress bar progress
    FINISHED_PROGRESS_STR = ose.get("FINISHED_PROGRESS_STR", "▰")
    UNFINISHED_PROGRESS_STR = ose.get("UNFINISHED_PROGRESS_STR", "▱")

    # API VARS
    # Get this value from https://free.currencyconverterapi.com/
    CURRENCY_API = ose.get("CURRENCY_API", None)
    # DeepAI value can get from https://deepai.org/
    DEEP_AI = ose.get("DEEP_AI", None)
    # Google Drive plugin for info: # TODO
    G_DRIVE_CLIENT_ID = ose.get("G_DRIVE_CLIENT_ID", None)
    G_DRIVE_CLIENT_SECRET = ose.get("G_DRIVE_CLIENT_SECRET", None)
    G_DRIVE_FOLDER_ID = ose.get("G_DRIVE_FOLDER_ID", None)
    G_DRIVE_DATA = ose.get("G_DRIVE_DATA", None)
    G_DRIVE_INDEX_LINK = ose.get("G_DRIVE_INDEX_LINK", None)
    # Genius lyrics get this value from https://genius.com/developers both has
    GENIUS_API = ose.get("GENIUS_API", None)
    # GitHub vars https://github.com/
    GITHUB_ACCESS_TOKEN = ose.get("GITHUB_ACCESS_TOKEN", None)
    GIT_REPO_NAME = ose.get("GIT_REPO_NAME", None)
    # This is required for the speech to text plugin. Get your USERNAME from
    # https://console.bluemix.net/docs/services/speech-to-text/getting-started.html
    IBM_WATSON_CRED_URL = ose.get("IBM_WATSON_CRED_URL", None)
    IBM_WATSON_CRED_PASSWORD = ose.get("IBM_WATSON_CRED_PASSWORD", None)
    # Get free API from https://dashboard.ipdata.co/sign-up.html
    IPDATA_API = ose.get("IPDATA_API", None)
    # Last.fm plugin vars for info: # TODO
    BIO_PREFIX = ose.get("BIO_PREFIX", None)
    LASTFM_API = ose.get("LASTFM_API", None)
    LASTFM_USERNAME = ose.get("LASTFM_USERNAME", None)
    LASTFM_PASSWORD_PLAIN = ose.get("LASTFM_PASSWORD", None)
    LASTFM_SECRET = ose.get("LASTFM_SECRET", None)
    # Get a Free API Key from OCR.Space
    OCRSPACE_API = ose.get("OCRSPACE_API", None)
    # Get your own APPID from https://api.openweathermap.org/data/2.5/weather
    OPEN_WEATHER_MAP_APPID = ose.get("OPEN_WEATHER_MAP_APPID", None)
    # Can get from https://coffeehouse.intellivoid.net/
    RANDOM_STUFF_API_KEY = ose.get("RANDOM_STUFF_API_KEY", None)
    # Get your own API key from https://www.remove.bg/
    RMBG_API = ose.get("RMBG_API", None)
    # Get your own ACCESS_KEY from http://api.screenshotlayer.com/api/capture for screenshot
    SCREEN_SHOT_LAYER_ACCESS_KEY = ose.get("SCREEN_SHOT_LAYER_ACCESS_KEY", None)
    # Spotify plugin vars for info: # TODO
    SP_DC = ose.get("SP_DC", None)
    SP_KEY = ose.get("SP_KEY", None)
    # SpamWatch API you can get it from get API from https://t.me/SpamWatchBot?start=token
    SPAMWATCH_API = ose.get("SPAMWATCH_API", None)
    # For transfer channel 2 step verification code of Telegram
    TG_2STEP_VERIFICATION_CODE = ose.get("TG_2STEP_VERIFICATION_CODE", None)
    # JustWatch Country for watch plugin
    WATCH_COUNTRY = ose.get("WATCH_COUNTRY", "TR")

    # DON'T EDIT BELOW THIS LINE IF YOU DON'T KNOW WHAT YOU ARE DOING
    # TG API limit. A message can have maximum 4096 characters!
    MAX_MESSAGE_SIZE_LIMIT = 4095
    # Specify LOAD and NO_LOAD
    LOAD = []
    # Warn mode for anti flood
    ANTI_FLOOD_WARN_MODE = ChatBannedRights(
        until_date=None, view_messages=None, send_messages=True
    )
    CHROME_BIN = ose.get("CHROME_BIN", "/app/.apt/usr/bin/google-chrome")
    CHROME_DRIVER = ose.get("CHROME_DRIVER", "/app/.chromedriver/bin/chromedriver")
    # For sed plugin
    GROUP_REG_SED_EX_BOT_S = ose.get(
        "GROUP_REG_SED_EX_BOT_S", r"(regex|moku|BananaButler_|rgx|l4mR)bot"
    )
    # time.py
    COUNTRY = str(ose.get("COUNTRY", ""))
    TZ_NUMBER = int(ose.get("TZ_NUMBER", 1))
    # For updater plugin
    UPSTREAM_REPO_BRANCH = ose.get("UPSTREAM_REPO_BRANCH", "DOGE")
    # DON'T TOUCH THIS AT ALL
    SUDO_USERS: Set[int] = set()
    DOGELOGO = None
    BOTLOG = False
    BOTLOG_CHATID = 0


class Production(Config):
    LOGGER = False


class Development(Config):
    LOGGER = True
